# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ModularIsBetter' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'PKHUD', '~>4.0'
  # Pods for ModularIsBetter

  target 'ModularIsBetterTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ModularIsBetterUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
